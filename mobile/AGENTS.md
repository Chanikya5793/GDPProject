# Expo HAS CHANGED

Read the exact versioned docs before writing any code. This project is on
**Expo SDK 57** (`expo: ^57.0.16`), so the docs to read are:

https://docs.expo.dev/versions/v57.0.0/

The API moves between majors. Do not answer from memory, and do not assume a
snippet found elsewhere is current.
